services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: investimi
      POSTGRES_USER: investimi
      POSTGRES_PASSWORD: investimi
    ports:
      - "5433:5432"
    volumes:
      - investimi_pgdata:/var/lib/postgresql/data

volumes:
  investimi_pgdata:

